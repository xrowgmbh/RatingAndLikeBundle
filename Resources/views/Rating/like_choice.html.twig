{% spaceless %}
    <div class="dcs-rating-container">
        {% set params = params|merge({'id' : id, 'value' : 10}) %}
        <a href="{{ url('dcs_rating_add_vote', params) }}" class="like {{ style }}"><i></i></a> {{ numVotes }}
    </div>
{% endspaceless %}